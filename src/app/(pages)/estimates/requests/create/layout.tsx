import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Schesti - Create Estimate',
    description: 'Schesti - Create Estimate',
}

export default function Layout({ children }: { children: React.ReactNode }) {
    return children
}
